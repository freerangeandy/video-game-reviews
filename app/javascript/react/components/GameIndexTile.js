import React from "react"

const GameIndexTile = props => {
   const testGameTitle = props.title

    return(
        <div>
            <h4> {testGameTitle} </h4>
        </div>
    )
}

export default GameIndexTile